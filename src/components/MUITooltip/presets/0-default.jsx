import * as React from 'react';
import MUITooltip from '../MUITooltip';

export default (
  <MUITooltip title="Tooltip copy" arrow placement="top" uxpId="mui-tooltip-default">
    Hover target
  </MUITooltip>
);
