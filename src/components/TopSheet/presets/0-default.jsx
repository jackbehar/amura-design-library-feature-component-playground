import * as React from 'react';
import TopSheet from '../TopSheet';

export default (
  <TopSheet isOpen={true} handleClose={() => {}} variant="top" uxpId="top-sheet-default" disableAutoClose={true}>
    Top sheet content
  </TopSheet>
);
